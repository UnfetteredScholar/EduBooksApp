<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edubooksapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduBooksApp">
        <activity android:name=".RequestBook"></activity>
        <activity android:name=".InfoTech" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.EduBooksApp.NoActionBar" />
        <activity android:name=".Math" />
        <activity android:name=".IT" />
        <activity android:name=".History" />
        <activity android:name=".Physics" />
        <activity android:name=".Chemistry" />
        <activity android:name=".Biology" />
        <activity android:name=".Homepage" />
        <activity android:name=".LogIn" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>